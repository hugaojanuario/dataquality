name: Release desktop

on:
  push:
    tags: ['v[0-9]+.[0-9]+.[0-9]+']

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  macos:
    runs-on: macos-14
    timeout-minutes: 30
    env:
      QT_QPA_PLATFORM: offscreen
      QT_QUICK_BACKEND: software
      DATAQUALITY_PYTHON: python
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - run: python -m pip install -e ".[dev,build,jdbc]"
      - run: python -m pytest -q
      - run: ./scripts/build-macos.sh --smoke-test
      - name: Prepare macOS installer
        run: |
          test "$(uname -m)" = "arm64"
          mv dist/DataQuality-macos-arm64.dmg dist/DataQuality-macOS-arm64.dmg
      - uses: actions/upload-artifact@v4
        with:
          name: release-macos
          path: dist/DataQuality-macOS-arm64.dmg
          if-no-files-found: error

  windows:
    runs-on: windows-latest
    timeout-minutes: 30
    env:
      QT_QPA_PLATFORM: offscreen
      QT_QUICK_BACKEND: software
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - run: python -m pip install -e ".[dev,build,jdbc]"
      - run: python -m pytest -q
      - shell: pwsh
        run: .\scripts\build-executable.ps1 -SmokeTest
      - name: Prepare Windows downloads
        shell: pwsh
        run: |
          Copy-Item dist\dataqualy.exe dist\DataQuality-Windows-x64.exe
          Compress-Archive -Path dist\DataQuality-Windows-x64.exe -DestinationPath dist\DataQuality-Windows-x64.zip -Force
      - uses: actions/upload-artifact@v4
        with:
          name: release-windows
          path: |
            dist/DataQuality-Windows-x64.exe
            dist/DataQuality-Windows-x64.zip
          if-no-files-found: error

  publish:
    needs: [macos, windows]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: release-*
          path: release
          merge-multiple: true
      - name: Generate checksums
        run: cd release && sha256sum * > SHA256SUMS.txt
      - name: Publish GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          if gh release view "$GITHUB_REF_NAME" >/dev/null 2>&1; then
            gh release upload "$GITHUB_REF_NAME" release/* --clobber
          else
            gh release create "$GITHUB_REF_NAME" release/* --verify-tag --generate-notes --title "DataQuality $GITHUB_REF_NAME"
          fi
